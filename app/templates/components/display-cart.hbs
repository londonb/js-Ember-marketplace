<div class="shoppingCart">
  <h4>Shopping Cart</h4>
  {{#each shoppingCart.items as |item|}}
  <li>{{item.name}} -- ${{item.cost}}</li>
  {{/each}}
  Total: ${{cartTotal}}
  <button class="btn btn-xs btn-success"{{action 'emptyCart'}}>Empty Cart</button>
</div>
